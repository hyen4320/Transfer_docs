---
tags:
  - daily
date: 2026-05-04
---
# 2026-05-04 (MON)

---

## 오늘의 목표

> 배포 반영, Report BE/FE 적용, FA 추적 기능, SNS 마케팅 실행 시작, 루머 확정 로직 설계

---

## 우선순위 & 진행 계획

### 현재 상태 (2026-05-04 업데이트)

| 목표 | 상태 | 비고 |
|---|---|---|
| 배포 반영 (Nginx + player sitemap) | 미착수 | 어제 코드 완성 |
| Report BE/FE 적용 | 완료 | empty state + 타입 검증 완료 |
| FA 추적 기능 | 완료 | 4곳 수정 + DB 마이그레이션 |
| Instagram 계정 개설 + 첫 게시물 | 미착수 | |
| 루머 확정 로직 설계 | 미착수 | |

---

### Priority 1 — 배포 반영

어제 구현한 코드 프로덕션 적용.

- [x] BE 빌드 + 배포 (`SitemapController`, `PlayerRepository` 변경)
- [x] Nginx 설정 업데이트 (`/sitemap-players.xml` location 블록)
- [x] Google Search Console에 `sitemap-players.xml` 제출
- [x] `/player/1` 리다이렉트 프로덕션 동작 확인

---

### Priority 2 — SNS 마케팅 (병행)

- [ ] Instagram 계정 개설 / 프로필 세팅
- [ ] 첫 게시물용 지도 스크린샷 캡처 (고화질)
- [ ] 첫 게시물 업로드

---

### Priority 3 — Report BE/FE 적용

FE 매거진 디자인은 완성. BE API 연결 및 실데이터 검증.

- [x] Report 탭 각 카드 API 실데이터 확인 (`/report/league-spending`, `/report/top-deals` 등)
- [x] 빈 응답 케이스 처리 (skeleton / empty state)
- [x] 시즌 값 계산 로직 검증 (`SEASON_OPTIONS` value 형식이 BE 파라미터와 일치하는지)

---

### Priority 4 — FA 추적 기능

기자 기사로 FA 전환 추적이 **현재 4군데에서 막혀 동작 안 함**. 아래 순서대로 수정 필요.

**막힌 지점:**
1. `PostParsingServiceImpl.java:47` — `toClub == null` 이면 스킵 → FA 보도 전부 누락
2. `TransferNews.Status` enum — `FREE_AGENT` 없음 → `parseStatus()`에서 RUMOR 폴백
3. `TransferNews.java:34` — `to_club_id nullable = false` → DB 레벨 저장 불가
4. `PostParsingServiceImpl.java:102` — FA 확정 시 `updateCurrentClub(null, FREE_AGENT)` 경로 없음

**수정 체크리스트:**
- [x] `TransferNews.Status`에 `FREE_AGENT` 추가
- [x] `TransferNews.toClub` → `@JoinColumn(nullable = true)` 변경
- [x] `PostParsingServiceImpl` — status가 FREE_AGENT일 때 toClub null 허용 분기 추가
- [x] `PostParsingServiceImpl` — FA CONFIRMED 시 `player.updateCurrentClub(null, FREE_AGENT)` 호출
- [x] DB 마이그레이션 SQL — `to_club_id` NOT NULL 제약 해제

---

### Priority 5 — 루머 확정 로직 설계

- [x] `updateStatus(Long newsId, Status status)` 엔드포인트 설계 → `PATCH /api/admin/news/{newsId}/confirm` 구현 완료
- [x] 수동 확정 vs 신뢰도 점수 자동 확정 방향 결정 → 수동 위주 + Gemini 보조 유지
- [x] `UnregisteredMention` 큐 구현 여부 결정 → 보류 (선수 DB 충분히 쌓인 후)

**구현 내용:**
- `TransferNews.updateStatus(Status)` 엔티티 메서드 추가
- `Verification.update()` 엔티티 메서드 추가
- `ConfirmRequest` DTO 추가 (`status`, `sourceUrl`, `confirmedByJournalistId`)
- `TransferNewsServiceImpl.confirm()` — Verification upsert + Player.currentClub 동기화
- `TransferNewsServiceImpl.updateStatus()` Builder 버그 수정 (season/window 유실 문제)
- `AdminController` `PATCH /api/admin/news/{newsId}/confirm` 추가 (X-Admin-Token 보호)
- `CorsConfig` PATCH 메서드 추가

---
### Priority 6 -- npm install 버그 수정

- [x] npm ci, npm install 오류 해결

---

## 오늘 한 일

### Report DATA/EDITORIAL 통합

- DATA 탭과 EDITORIAL 탭을 단일 그리드로 통합
- BE: `generateDataReports(Short season)` 구현 — 6개 집계 쿼리 결과를 `editorial_report` 테이블에 `type=DATA, status=PUBLISHED`로 저장
  - `EditorialReportRepository`에 `deleteByTypeAndTagsContaining()` 추가 (시즌별 재생성 지원)
  - `blocks` 필드에 `kind: "data-raw"` 블록으로 직렬화
  - 덱(deck) 문구를 Java에서 동적 생성 (리그명·이적료 등 실데이터 삽입)
  - `POST /api/report/generate?season=49` 엔드포인트 추가
- FE: `ReportPage.tsx` 전면 리팩터링
  - 탭 제거, `buildCards()` / `MagazineCard` / `DetailView` 삭제
  - `fetchEditorialReports()` 단일 API 호출로 통합
  - `data-raw` 블록 카테고리별 렌더러 추가 (league-spending, top-deals, position-trends, club-activity, transfer-flow, free-agents)
  - `editorialReport.ts` Block 유니언에 `data-raw` 타입 추가
- 최초 1회 `POST /api/report/generate?season=49` 호출 필요 (자동 실행 보류)

---

## 배운 것 / 메모

---

## 막힌 것 / 내일 볼 것

---

## 링크

- [[📋 Overview]]
- [[dev-plan]]
- [[📌 Kanban]]
- [[2026-05-03]]
