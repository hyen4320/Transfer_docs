---
tags:
  - daily
date: 2026-05-29
---
`# 2026-05-29 (THU)

---

## 오늘의 목표

> FE 개선

---

## 우선순위 & 진행 계획

| 우선순위 | 예상 시간 |
|---------|---------|
| P1 — player 단독 페이지 홈 유도 | 30분 |
| P2 — report 광고 적용 | 20분 |
| P3 — fixture 광고 적용 | 20분 |
| P4 — fixture 동작 원리 파악 | 30분 |

---

### Priority 1 — player 단독 페이지 개선

> 검색엔진에서 `/players/78`로 직접 유입된 사용자를 홈으로 유도

**현황**: `PlayerDetailPage.tsx:82` Back 버튼이 `navigate(-1)` → 외부 유입 시 히스토리 없음

- [x] Back 버튼: `window.history.length <= 1` 이면 `/`로 navigate, 아니면 `navigate(-1)`
- [x] 헤더 우측에 "Explore TransferMap →" CTA 추가 (옵션 A)

---

### Priority 2 — report 페이지에 광고 적용

> `ReportPage.tsx` — 현재 AdSlot 전혀 없음

사용 가능 슬롯: `LEADERBOARD`(728×90), `FEED_NATIVE`(320×100) x3, `ANCHOR_BOTTOM`(320×50)

- [x] `import AdSlot, { SLOT } from './AdSlot'` 추가
- [x] 리포트 목록 상단 → `LEADERBOARD` (데스크탑, md:block) / `FEED_NATIVE` (모바일, md:hidden)
- [x] 카드 사이 3번째마다 → `FEED_NATIVE_2` 삽입 (gridColumn: 1 / -1 풀폭)
- [x] 개별 리포트 페이지 하단 → `FEED_NATIVE_3`

---

### Priority 3 — Fixture 페이지에 광고 적용

> `FixturesPage.tsx` — 현재 AdSlot 전혀 없음, P2와 동일 슬롯 풀

- [x] `import AdSlot, { SLOT } from './AdSlot'` 추가
- [x] 리그 필터 위 상단 → `LEADERBOARD`
- [x] 경기 목록(Elsewhere this week) 4번째마다 → `FEED_NATIVE` (gridColumn: 1 / -1)

---

### Priority 4 — fixture 동작 원리 파악

> `FixturesPage.tsx`가 5개 API 함수 + `MOCK_FIXTURES` 폴백 구조 — 흐름 파악 필요

- [x] `src/api/fixtures.ts` 엔드포인트 확인
- [x] BE `/api/fixtures` 실제 데이터 제공 여부 확인
- [x] API 실패 시 mock 폴백 로직 흐름 정리

#### FE 엔드포인트 (`src/api/fixtures.ts`)

| 함수 | 메서드 | 경로 |
|------|--------|------|
| `fetchFixtures(leagueId, date?)` | GET | `/fixtures?leagueId&date` |
| `fetchWeekFixtures(leagueId, from?)` | GET | `/fixtures/week?leagueId&from` |
| `fetchStandings(leagueId, season?)` | GET | `/standings/{leagueId}` |
| `fetchMatchEvents(fixtureId)` | GET | `/fixtures/{id}/events` |
| `fetchMatchStats(fixtureId)` | GET | `/fixtures/{id}/stats` (catch → null) |
| `fetchMatchLineups(fixtureId)` | GET | `/fixtures/{id}/lineups` |

#### BE 구현 상태 (`FixturesController` + `FixturesServiceImpl`)

- **구현됨**: `getFixtures`, `getFixturesByWeek`, `getStandings`
  - Redis 캐시 (fixtures 5분, standings 1시간)
  - football-data.org API 호출 (`/competitions/{code}/matches`, `/standings`)
  - LEAGUE_MAP: pl→PL, ll→PD, bl→BL1, sa→SA, l1→FL1
  - RestClientException 시 빈 리스트 반환
- **미구현 (빈 응답)**:
  - `getMatchEvents` → `List.of()`
  - `getMatchStats` → `null` (Controller에서 204 No Content)
  - `getMatchLineups` → `List.of()`

#### 폴백 흐름

1. `FixturesPage.tsx:807` `useState(MOCK_FIXTURES)` — 초기값은 항상 mock
2. `useEffect` → `fetchWeekFixtures` 호출. 결과 length > 0이면 `setFixtures(data)`
3. **빈 응답이면 mock 유지** (`if (alive && data.length)` 조건)
4. 네트워크 에러는 `.catch(() => {})` → 그대로 mock 유지
5. Standings도 동일: 빈 응답/에러 → `MOCK_STANDINGS[league]`
6. 상세 탭 — events/lineups는 BE 미구현이라 항상 빈 배열 → UI에 "NO EVENTS / LINEUP UNAVAILABLE"

#### 개선 포인트 (다음 작업 후보)

- events/stats/lineups 미구현 — football-data.org 무료 플랜에 해당 데이터 없음. API 교체 필요
- `currentSeason()` 로직이 Controller / ServiceImpl 중복 — util로 추출 가능
- `leagueFilter === 'all'`일 때 'pl'만 호출 — 다른 리그도 합쳐서 보여줘야 진짜 'all'

---

## 배운 것 / 메모


---

## 막힌 것 / 내일 볼 것

- [ ] 

---

## 링크

- [[📋 Overview]]
- [[dev-plan]]
- [[📌 Kanban]]
- [[Marketing/SNS]]
- [[2026-05-08]]
