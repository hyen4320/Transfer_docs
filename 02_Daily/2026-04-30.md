---
tags:
  - daily
date: 2026-04-30
---
# 2026-04-30 (THU)

---

## 오늘의 목표

> 지금까지 진행상황 문서화, FE개선, Trending 구현, 루머 확정 로직 개발

---

## 우선순위 & 진행 계획

### 현재 상태 (2026-04-30 업데이트)

| 목표 | 상태 | 비고 |
|---|---|---|
| FE 개선 | 완료 | 뉴스피드 시즌 연동, 필터 버그 수정 |
| 진행상황 문서화 | 진행 중 | 오늘 한 일 섹션 작성 필요 |
| Trending 구현 | 미착수 | BE/FE 모두 남음 — 오늘 최우선 |
| 루머 확정 로직 | 초안 완료 | 수집 스케줄러만, 확정·저장 로직 없음 |

**남은 핵심 작업 (우선순위 순):**
1. Trending BE → FE (독립 기능, 빠른 완성 가능)
2. 루머 확정 로직 (X API 수집 → DB 저장 → 상태 필드 설계)
3. 진행상황 문서화 (오늘 한 일, 배운 것 섹션 채우기)
4. Canonical URL (SEO, 낮은 난이도 — 자투리 시간 활용)

---

### Priority 1 — DB 좌표 수정 적용 & 어제 FE 변경 검증

#### HeidiSQL 스크립트 실행
- [x] `DB/fix_club_coordinates.sql` HeidiSQL에서 실행 (35개 구단 좌표 수정)
- [x] WorldMap에서 피오렌티나(피렌체), 페네르바체(이스탄불), 레알 베티스(세비야) 위치 확인

#### 어제 FE 변경사항 브라우저 확인
- [x] 모바일에서 리그 동그라미 절반 크기 확인 (26px / sm:52px)
- [x] 클럽 뷰 시즌 선택기 동작 — Incoming/Outgoing 시즌별 필터링-안되고 있음
- [x] 이적시장 닫힌 시즌(25/26) → 이적료순 정렬 확인
- [x] 튀르키예 지도 렌더링 확인
#### FE개선
- [x] 뉴스피드에서 시즌 선택시 메인페이지 시즌 업데이트 지금 뉴스피드에서 시즌을 선택해도 메인페이지에 구단 마커는 변동이 없음.
- [x] 모바일·데스크톱 기타 버그들 수정
    - `CountryMapPage` — 시즌 선택기 없어서 리그 지도 진입 후 시즌 변경 불가 → 로컬 시즌 선택기 추가
    - `CountryMapPage` — 상태 필터(interest/rumour/confirmed 등) 없어서 이적선/뉴스 필터링 불가 → 상태 필터 추가
    - `CountryMapPage` — 시즌·상태 필터 변경 시 이적선이 갱신되지 않던 버그 → `projRef` + `computeRoutes` effect 분리로 수정
    - `LeftPanel` — `h-screen`으로 모바일 브라우저 주소창에 패널 하단이 잘리던 버그 → `h-[100dvh]` 수정
    - 뉴스피드에서 선수 클릭 시 CountryMap 전체가 열리던 버그 → PlayerPanel만 교체/유지하도록 수정

---

### Priority 2 — Trending 기능 구현  ← 오늘 최우선 착수

#### BE (순서대로 진행)
- 메모만 있고 실제 구현 없음, FE는 더미 상수 `TRENDING` 사용 중
- [x] `TrendingService` — Redis ZSet 기반 집계로 구현 (뉴스 테이블 쿼리 대신 Redis 검색 기록 집계)
- [x] `TrendingController` — `GET /api/news/trending-players?days=7&limit=5` (NewsController 내 구현, POST record endpoint 포함)
- [x] `@Scheduled` 주간 초기화 (매주 월 자정) — `@Cacheable` Caffeine은 Redis로 대체됨

#### FE (BE 완료 후 진행)
- [x] `src/api/` — `fetchTrendingPlayers()` 함수 추가 (news.ts)
- [x] SidePanel Search 뷰에서 마운트 시 호출 → `TRENDING` 상수 폴백 유지하며 API 호출로 대체

---

### Priority 3 — X(Twitter) API 수집 & 루머 확정 로직 개발

- [x] X API v2 Bearer Token 발급 확인
- [x] 이적 루머 키워드 기반 tweet 수집 엔드포인트 설계
- [x] BE 수집 스케줄러 초안 작성
- [ ] 수집된 트윗 → DB 저장 스키마 확정 (rumor_status: pending / confirmed / denied)
- [ ] 루머 확정 로직 — 신뢰도 점수 or 수동 확정 엔드포인트 설계
- [ ] `UnregisteredMention` 큐 구현 여부 결정 (보류 항목과 연계)

---

### Priority 4 — Redis 적용 검토

- [x] 현재 Caffeine 적용 범위 파악
- [x] Caffeine vs Redis 기준 비교 (분산 필요성, 재시작 시 캐시 유지)
- [x] 결론 정리

---

### Priority 5 — SEO / 인프라
#### 파비콘 (구글 규격)
- [x] 512×512 PNG 준비, `manifest.json` 업데이트, `index.html` 정리

#### Cloudflare WAF
- [x] WAF 규칙 설정 (계속 이월 중)

---

### 보류 / 결정 필요

- Filter `★ Save` 버튼 — 저장 기능 구현할지, 제거할지 결정 필요
- `UnregisteredMention` 큐 구현 — Gemini 파싱 후 선수/구단 미등록 시 스킵 대신 별도 테이블에 저장, 수동 검토용 엔드포인트 추가 고민 중

---

## 오늘 한 일

- Filter Clear 버그 수정 — `onClearFilter` prop 추가로 `filteredNews` 초기화 경로 연결
- `toClubName` null → `'Free Agent'` 폴백 (`api/types.ts`, `api/mappers.ts`)
- `bundesliga.sql` 헤더 오염 텍스트 제거
- WorldMap Profiler 3회 측정 → 2건 최적화 적용
  - `draw()` 3 setState 배칭 → 평균 렌더 54ms → 38ms
  - `tooltipPos` ref 전환 → mousemove 연속 렌더 20회 제거

---

## 배운 것 / 메모

---

## 막힌 것 / 내일 볼 것

---

## 링크

- [[📋 Overview]]
- [[dev-plan]]
- [[📌 Kanban]]
- [[2026-04-27]]
