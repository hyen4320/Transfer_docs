---
tags:
  - daily
date: 2026-04-25
---
# 2026-04-25 (SAT)

---

## 오늘의 목표

>

---

## 우선순위 & 진행 계획

> 오늘 목표: ****
> 완료: 

---

### Priority 1 — 데이터 추가, DB 스키마 점검

- [x] 기존 transfer_news 데이터 삭제
- [x] tm_league_history.py 완료 확인 → league_history.sql 생성됐는지 체크
- [x] DB에 V4__club_season.sql DDL 적용
- [x] DB에 league_history.sql 적용 (역대 구단 + club_season + 위치정보, 2530건 확인)
- [ ] python DB/kaggle_import.py 실행 → kaggle_transfers.sql 생성 (코드 수정 완료, 재실행 필요)
- [ ] DB에 kaggle_transfers.sql 적용
- [ ] Google Search Console 도메인 소유권 인증 (TXT 레코드 또는 메타태그)
- [ ] claude design 적용
- [ ] cloudflare waf 적용

---

### Priority 2 — X 활성화 계정 추리기

- [ ] X 사용 안하는 계정 추리기 - 다른 방식의 데이터 수집 고려

---

### Priority 3 — 구글 애드샌스 신청 및 적용

- [x] 구글 애드샌스 신청 및 적용
- [x] 구글 애드샌스 적용한 디자인 고민

---

### Priority 4 — FE: 모바일 최적화

- [ ] SidePanel `w-[460px]` → 모바일에서 full-width / bottom sheet 처리
- [ ] LeftPanel 모바일 대응
- [ ] Topbar 버튼 4개 → 모바일 햄버거 메뉴 또는 재배치
- [ ] WorldMap D3 터치 제스처 (pinch zoom / drag) 확인

---

### Priority 5 — Redis 활용법 조사 및 적용

- [ ] 현재 Redis 사용 현황 파악 (캐시 / 세션 / 기타)
- [ ] 적용 가능한 캐싱 전략 정리
- [ ] 실제 적용 여부 결정

---

### Priority 6 — Elasticsearch 도입 여부 검토 (여유 있을 때)

- [ ] Elasticsearch 도입 비용 대비 효과 검토

---

### 오늘 건드리지 않을 것

- 

---

## 오늘 한 일

### FE — 데스크톱 UX 최적화 + 버그 수정

**버그 수정**
- `ErrorBoundary`가 `<BrowserRouter>` 바깥에 있어서 `useNavigate()` 크래시 → `<BrowserRouter>`로 `<ErrorBoundary>` 감싸도록 `main.tsx` 수정
- `SidePanel`에서 `useRef` import 누락 → 추가

**데스크톱 UX — 양방향 연동 (지도 ↔ 뉴스피드)**
- WorldMap 이적 루트 hover 시 `onRouteHover(id)` 콜백 발생
- App에서 `hoveredRouteId` state로 lift → SidePanel에 전달
- SidePanel: `hoveredRouteId` 바뀌면 해당 뉴스카드로 `scrollIntoView` + 파란 ring 하이라이트

**데스크톱 UX — 키보드 단축키**
- `Esc` : 사이드패널 + 검색패널 모두 닫기
- `/` : 검색패널 토글 (열릴 때 선수검색 탭 자동 포커스)
- `n` : 뉴스피드 토글
- `LeftPanel`을 `forwardRef` + `useImperativeHandle`로 변경해 `focusSearch()` 외부 노출

**데스크톱 UX — 토글 동작**
- News Feed 버튼 / `n` 키: 이미 열려있으면 닫힘 (toggle)
- Search 버튼 / `/` 키: 동일하게 toggle
- 활성 버튼에 파란 하이라이트 (border-accent + bg-accent/10)

**Topbar 레이아웃 변경**
- TransferMap 로고: 왼쪽 고정
- 버튼 4개 (News Feed / Journalists / Notice / Search): 중앙 배치 (`flex-1 justify-center`)
- 우측 여백 spacer로 시각적 균형 맞춤
- 모바일은 기존 유지 (로고 중앙, 버튼 탭바)

---

## 배운 것 / 메모

### Google AdSense 적용 — 배치만으론 부족, 추가 작업 필요

**필수 작업**

1. **`ads.txt`** — `public/ads.txt` 에 아래 한 줄 추가 (없으면 수익 거의 0)
   ```
   google.com, pub-6322996609513242, DIRECT, f08c47fec0942fa0
   ```

2. **슬롯 ID 교체** — AdSense 콘솔 → 광고 단위 → 새 광고 단위 생성 후 발급된 10자리 ID를 `FE/transfermap/src/components/AdSlot.tsx`의 `SLOT` 상수에 교체
   ```ts
   ANCHOR_BOTTOM:    '실제ID',
   FEED_NATIVE:      '실제ID',
   SKYSCRAPER:       '실제ID',
   // ...
   ```

3. **사이트 검수 완료** — AdSense 콘솔에서 사이트 상태 "준비 완료" 확인

**구현된 광고 배치 위치**

| 위치 | 슬롯 |
|------|------|
| 뉴스피드 3번째 카드마다 | FEED_NATIVE |
| Journalist 리스트 헤더 아래 | LEADERBOARD |
| Journalist 테이블 4행마다 | TABLE_ROW_NATIVE |
| Journalist / Player 우측 사이드 | MPU_SIDEBAR_1/2 |
| Player 타임라인↔뉴스 사이 | INTER_SECTION |
| 메인맵 하단 앵커 | ANCHOR_BOTTOM |
| 메인맵 우측 스카이스크래퍼 | SKYSCRAPER |

---

## 막힌 것 / 내일 볼 것

- kaggle_transfers.sql 적용 전 DB 정리 → 재생성 → 재적용 필요
- AdSense 슬롯 ID는 승인 후 교체

---

## 링크

- [[📋 Overview]]
- [[dev-plan]]
- [[📌 Kanban]]
